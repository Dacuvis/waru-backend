[phases.setup]
nixPkgs = ["nodejs_20", "bun"]

[phases.install]
cmds = [
  "bun --version",
  "bun install --frozen-lockfile"
]

[phases.build]
cmds = [
  "bun run typecheck"
]

[start]
cmd = "bun run index.ts"
