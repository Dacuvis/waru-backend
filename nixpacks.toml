[phases.setup]
nixPkgs = ["nodejs_20", "unzip", "curl"]

[phases.install]
cmds = [
  "curl -fsSL https://bun.sh/install | bash",
  "~/.bun/bin/bun install"
]

[start]
cmd = "~/.bun/bin/bun run index.ts"